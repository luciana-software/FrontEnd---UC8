//Sistema para um radar de velocidade
//...... Aqui, = 111, ele vai ler a condição do if, aqui vai retornar você foi multdo e você esta dentro do limite de velocidade ......

//Variaveis
/*let velocidade;

//entrada
velocidade = 111;

//Processamento / Saída
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    console.log("Você foi multado!!!")
}

if(velocidade > 100 && velocidade <110){
    console.log("Você está sendo notificado por andar acima do limite")
}
else{
    console.log("você está dentro do limite de velocidade")
}
//Saída
*/

//.......................................

//Sistema para um radar de velocidade
//...... Aqui com = 111, usando if else na mesma linha, ele vai retornar você foi multado.

//Variaveis
/*let velocidade;

//entrada
velocidade = 111;

//Processamento / Saída
//É possivel ter um if com o else mas não é possivel ter um else sem um if.
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    console.log("Você foi multado!!!")
}
else if (velocidade > 100 && velocidade < 110){ 
        // && significa and no visual studio code
    console.log("Você está sendo notificado por andar acima do limite")
}
else{
    console.log("você está dentro do limite de velocidade")
}
//Saída
*/

//.......................................

//Sistema para um radar de velocidade
//...... Aqui com = 105, ele vai ler o if else e retornar você está sendo notificado por andar acima do limite ......

//Variaveis
/*let velocidade;

//entrada
velocidade = 105;

//Processamento / Saída
//É possivel ter um if com o else mas não é possivel ter um else sem um if.
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    console.log("Você foi multado!!!")
}
else if (velocidade > 100 && velocidade < 110){ 
        // && significa and no visual studio code
    console.log("Você está sendo notificado por andar acima do limite")
}
else{
    console.log("você está dentro do limite de velocidade")
}
//Saída
*/

//Sistema para um radar de velocidade
//...... Aqui com = 99, com o if else na mesma linha ele vai retornar você está dentro do limite de velocidadev......

//Variaveis
/*let velocidade;

//entrada
velocidade = 99;

//Processamento / Saída
//É possivel ter um if com o else mas não é possivel ter um else sem um if.
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    console.log("Você foi multado!!!")
}
else if (velocidade > 100 && velocidade < 110){ 
        // && significa and no visual studio code
        console.log("Você está sendo notificado por andar acima do limite")
}
else{
        console.log("você está dentro do limite de velocidade")
}

//Saída
*/

//.......................................

//Sistema para um radar de velocidade
//...... Aqui com = 99, com o if e o else em linhas diferentes, ele vai retornar a mesma mensagem: você está dentro do limite de velocidadev......

//Variaveis
/*let velocidade;

//entrada
velocidade = 99;

//Processamento / Saída
//É possivel ter um if com o else mas não é possivel ter um else sem um if.
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    console.log("Você foi multado!!!")
}
else 
    {
        if (velocidade > 100 && velocidade < 110){ 
            // && significa and no visual studio code
            console.log("Você está sendo notificado por andar acima do limite")
        }
        else{
            console.log("você está dentro do limite de velocidade")
        }
    }
//Saída
*/

//.......................................

//Sistema para um radar de velocidade
//...... codigo para rodar o html que ele criou como exemplo na aula.
//..... Esse código vai rodar o html, testei e funcionou aqui .....

//Variaveis
let velocidade;

//entrada
velocidade = Number(prompt("Digite a velocidade!!!"))

//Processamento / Saída
//É possivel ter um if com o else mas não é possivel ter um else sem um if.
if (velocidade > 110){
    // Só entra valores no limite maior que 100
    //console.log("Você foi multado!!!")
    alert("Você foi multado")
}
else 
    {
        if (velocidade > 100 && velocidade < 110){ 
            // && significa and no visual studio code
            //console.log("Você está sendo notificado por andar acima do limite")
            alert("Você está sendo notificado por andar acima do limite")
        }
        else{
            //console.log("você está dentro do limite de velocidade")
            alert("Você está dentro do limite de velocidade")
        }
    }
//Saída


